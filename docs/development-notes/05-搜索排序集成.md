很好，进入**第五步**。
这一步不再扩主链路，而是把第一阶段补完整，让它在“已有历史数据”的前提下真正能用起来。

## 第五步目标

做 3 件事：

1. **把历史 `Usage` 初始化成 `service_trust_stats`**
2. **补一个手动测试脚本，验证“调用 → 评价 → trust 更新 → 搜索展示”闭环**
3. **顺手给你整理一版可以直接写进论文实现章节的系统说明**

这一步做完后，你这套第一阶段功能就算真正闭环了。

------

# 第一部分：初始化历史 trust 摘要

因为你已经接受：

- 历史 `Usage` 参与客观分
- 历史没有评价不补主观分

所以现在最合理的方式就是：
**做一个手动回填脚本**，把现有公开服务的 `ServiceTrustStats` 先算出来。

这样做比塞进启动流程更稳，原因很简单：

- 你现在没有调度/后台任务体系
- 启动时强行全量重算会让应用职责变重
- 手动脚本更符合你现在项目的阶段

------

## 新建文件：项目根目录 `backfill_trust_stats.py`

注意，这个文件放在**项目根目录**，和你现在那些 `test_scenario*.py` 一样。

请完整新建下面这份代码：

```python
#!/usr/bin/env python3
"""
历史信任摘要回填脚本

用途：
1. 基于现有的Usage历史，为公开服务初始化 ServiceTrustStats
2. 不伪造历史主观评价，仅回填客观分与已有评价汇总结果
"""

import sys
import os
from datetime import datetime

# 确保可以导入 app 模块
CURRENT_DIR = os.path.dirname(os.path.abspath(__file__))
if CURRENT_DIR not in sys.path:
    sys.path.insert(0, CURRENT_DIR)

from app.database import SessionLocal
from app.models.service import Service
from app.services.trust_service import TrustService


def main():
    """
    主执行入口
    """
    db = SessionLocal()

    try:
        trust_service = TrustService(db)

        # 仅对公开且激活的服务做初始化
        services = db.query(Service).filter(
            Service.is_active == True,
            Service.is_public == True
        ).all()

        print(f"[INFO] 开始回填公开服务信任摘要，共 {len(services)} 个服务")

        success_count = 0
        failed_count = 0

        for service in services:
            try:
                trust_stats = trust_service.recompute_service_trust(service.id)
                print(
                    f"[OK] service_id={service.id}, "
                    f"name={service.name}, "
                    f"trust_score={trust_stats.trust_score}, "
                    f"usage_count={trust_stats.usage_count}, "
                    f"rating_count={trust_stats.rating_count}"
                )
                success_count += 1

            except Exception as e:
                print(f"[ERROR] service_id={service.id}, name={service.name}, error={e}")
                failed_count += 1

        print("\n========== 回填完成 ==========")
        print(f"成功: {success_count}")
        print(f"失败: {failed_count}")
        print(f"完成时间: {datetime.utcnow().isoformat()}")

    finally:
        db.close()


if __name__ == "__main__":
    main()
```

------

## 这一步怎么运行

在项目根目录执行：

```bash
python backfill_trust_stats.py
```

如果你当前环境里是 `python3`，就用：

```bash
python3 backfill_trust_stats.py
```

------

## 运行成功后你要验证的东西

### 验证 1：脚本输出

应该看到类似：

- 一共有多少公开服务
- 每个服务的 `trust_score`
- `usage_count`
- `rating_count`

------

### 验证 2：数据库里 `service_trust_stats` 有数据

至少应该确认：

- 已有调用历史的公开服务，都出现了 `service_trust_stats`
- 没有主观评价时：
  - `subjective_score = 0`
  - `trust_score` 主要来自客观分

------

# 第二部分：补一个第一阶段闭环测试脚本

你现在已经有：

- 调用能落 `usage`
- 评价能提交
- trust 能计算
- 搜索能展示和重排

所以现在最合适的是新增一个**手动闭环测试脚本**。

------

## 新建文件：项目根目录 `test_scenario4_reviews.py`

请完整新建下面这份代码：

```python
#!/usr/bin/env python3
"""
场景4：服务评价与信任分闭环测试脚本

测试目标：
1. 调用服务
2. 从响应头中拿到 usage_id
3. 提交评价
4. 查询 trust 摘要
5. 再次搜索，观察 trust 字段是否可见
"""

import requests
import json

BASE_URL = "http://localhost:8001/api/v1"

USERNAME = "testuser"
PASSWORD = "testpassword123"


def login():
    """
    登录并获取访问令牌
    """
    url = f"{BASE_URL}/auth/login"
    data = {
        "username": USERNAME,
        "password": PASSWORD
    }
    resp = requests.post(url, json=data)
    assert resp.status_code == 200, f"登录失败: {resp.text}"
    token = resp.json()["access_token"]
    return token


def call_service(token: str):
    """
    调用测试服务，并返回 usage_id / request_id
    """
    url = f"{BASE_URL}/client/services/call"
    headers = {
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json"
    }
    data = {
        "agentdns_url": "agentdns://test-org/text/test-service",
        "input_data": {"text": "hello trust review"},
        "method": "POST"
    }

    resp = requests.post(url, headers=headers, json=data)

    # 允许失败响应也带追踪头
    usage_id = resp.headers.get("X-AgentDNS-Usage-ID")
    request_id = resp.headers.get("X-AgentDNS-Request-ID")

    print(f"[CALL] status={resp.status_code}")
    print(f"[CALL] usage_id={usage_id}, request_id={request_id}")
    print(f"[CALL] body={resp.text}")

    assert usage_id is not None, "调用响应头中未返回 usage_id"
    assert request_id is not None, "调用响应头中未返回 request_id"

    return int(usage_id)


def submit_review(token: str, usage_id: int):
    """
    针对本次调用提交评价
    """
    # 先查 usage，拿 service_id，避免后面查 summary 时不知道 service_id
    usage_url = f"{BASE_URL}/billing/usage"
    headers = {"Authorization": f"Bearer {token}"}
    usage_resp = requests.get(usage_url, headers=headers)
    assert usage_resp.status_code == 200, f"查询usage失败: {usage_resp.text}"

    usage_records = usage_resp.json()
    target_usage = None
    for item in usage_records:
        if item["id"] == usage_id:
            target_usage = item
            break

    assert target_usage is not None, f"未找到 usage_id={usage_id} 的使用记录"
    service_id = target_usage["service_id"]

    review_url = f"{BASE_URL}/reviews/"
    review_data = {
        "usage_id": usage_id,
        "outcome": "success",
        "task_fit": 5,
        "output_quality": 4,
        "protocol_adherence": 5,
        "would_reuse": True,
        "cost_satisfaction": 4,
        "feedback_text": "第一阶段闭环测试评价",
        "evidence": {
            "schema_valid": True,
            "downstream_success": True
        }
    }

    review_resp = requests.post(review_url, headers=headers, json=review_data)
    print(f"[REVIEW] status={review_resp.status_code}")
    print(f"[REVIEW] body={review_resp.text}")

    assert review_resp.status_code == 200, f"提交评价失败: {review_resp.text}"

    return service_id


def check_trust_summary(token: str, service_id: int):
    """
    查询服务信任摘要
    """
    url = f"{BASE_URL}/reviews/services/{service_id}/summary"
    headers = {"Authorization": f"Bearer {token}"}

    resp = requests.get(url, headers=headers)
    print(f"[SUMMARY] status={resp.status_code}")
    print(f"[SUMMARY] body={resp.text}")

    assert resp.status_code == 200, f"查询信任摘要失败: {resp.text}"

    data = resp.json()
    assert "trust_score" in data
    assert "success_rate" in data
    assert "rating_count" in data

    return data


def search_services(token: str):
    """
    再次搜索，观察返回结果是否带 trust 字段
    """
    url = f"{BASE_URL}/discovery/search"
    headers = {
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json"
    }
    data = {
        "query": "text service",
        "limit": 10,
        "sort_by": "balanced",
        "include_trust": True
    }

    resp = requests.post(url, headers=headers, json=data)
    print(f"[SEARCH] status={resp.status_code}")
    print(f"[SEARCH] body={resp.text}")

    assert resp.status_code == 200, f"搜索失败: {resp.text}"

    result = resp.json()
    assert "tools" in result

    if result["tools"]:
        first_tool = result["tools"][0]
        assert "trust_score" in first_tool
        assert "success_rate" in first_tool
        assert "rating_count" in first_tool

    return result


def main():
    token = login()
    usage_id = call_service(token)
    service_id = submit_review(token, usage_id)
    summary = check_trust_summary(token, service_id)
    search_result = search_services(token)

    print("\n========== 测试完成 ==========")
    print(f"service_id={service_id}")
    print(f"trust_score={summary['trust_score']}")
    print(f"rating_count={summary['rating_count']}")
    print(f"search_results={len(search_result.get('tools', []))}")


if __name__ == "__main__":
    main()
```

------

## 这一步怎么跑

```bash
python test_scenario4_reviews.py
```

------

## 这个脚本主要验证什么

它会帮你确认四条关键事实：

1. **调用响应头里真的有 `usage_id / request_id`**
2. **这条 usage 可以马上被评价**
3. **评价后 trust 摘要会立刻更新**
4. **搜索结果里 trust 字段会立刻可见**

这对你论文后面写“系统闭环”很重要。

------

# 第三部分：你论文实现章节现在可以这样写

这一部分你先不用写进代码，我先帮你把系统实现逻辑定稿。
后面你写论文时，可以直接按这个结构展开。

------

## 1. 功能定位

你现在新增的不是普通评分模块，而是：

**面向 Agent 服务调用场景的结构化评价与服务信任摘要机制。**

它建立在 AgentDNS 原有能力之上：

- 服务注册
- 服务发现
- 代理调用
- 统一认证
- 统一计费

并进一步补上：

- 调用后结构化回评
- 基于真实使用记录的服务级 trust 摘要
- 面向搜索结果的可信重排

------

## 2. 数据流

系统数据流可以概括成：

**服务调用 → Usage 锚点 → 可选结构化评价 → trust 聚合 → discovery 重排**

这条链比“单独评论系统”更合理，因为：

- 评价必须绑定真实调用
- 评价结果服务于服务选择
- trust 是 discovery 的增强层，不是独立产品

------

## 3. 评价模型

评价采用**结构化字段为主、文本反馈为辅**的形式：

- `outcome`
- `task_fit`
- `output_quality`
- `protocol_adherence`
- `would_reuse`
- `cost_satisfaction`
- `feedback_text`
- `evidence`

这种设计更适合 Agent 自动回评，也便于后续做定量聚合。

------

## 4. 信任分设计

第一阶段信任分采取混合方案：

- 客观分：来自 `Usage`
- 主观分：来自 `ServiceReview`
- 综合 trust：二者加权

第一阶段权重是：

- 客观分占主导
- 主观分作为增强项

这样更符合你当前项目还处于早期阶段、评价数据量有限的现实。

------

## 5. 搜索重排思想

你前面已经认可的核心思想，可以这样写：

> 系统首先依据语义检索和条件过滤确保候选服务满足任务需求，再在候选集内部基于 trust、性能与价格进行二次重排。

也就是：

- **需求匹配优先**
- **trust 负责候选重排**
- **价格和性能做辅助修正**

这个定位很重要，能避免论文里被写成“信任分替代服务匹配”。

------

# 你现在要做的事

先做这两件：

1. 运行 `backfill_trust_stats.py`
2. 运行 `test_scenario4_reviews.py`

然后把结果告诉我，至少告诉我：

- 回填脚本是否成功
- 测试脚本是否跑通
- 有没有哪个接口在实际联调时出现字段对不上、报错或数据没更新

如果这一步跑通，我下一步会带你做**第六步：收尾与加固**，重点是：

- 修第一阶段里最值得顺手修的小问题
- 给你整理一版“当前系统最终结构图 + 接口列表 + 论文实验点设计”