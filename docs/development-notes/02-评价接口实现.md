很好，进入**第二步**。
这一步我们把**评价接口骨架和信任分计算骨架**搭起来，让系统先具备：

- 提交/更新一次真实调用的评价
- 查询某个服务的 trust 摘要
- 支持 JWT 用户和 `agent_` key 两种身份提交评价
- 评价提交后自动重算该服务的 trust 摘要

这一步**先不改 proxy**，也不改 search 排序。
先把“评价”和“信任分摘要”这套基础链路跑起来。

------

# 第二步目标

完成这 5 件事：

1. 新建 `app/schemas/review.py`
2. 新建 `app/services/trust_service.py`
3. 给 `app/api/deps.py` 增加 `get_current_principal`
4. 新建 `app/api/reviews.py`
5. 在 `app/main.py` 注册 reviews 路由

------

# 1）新建文件：`app/schemas/review.py`

请完整新建下面这份代码。

```python
from datetime import datetime
from typing import Optional, Dict, Any

from pydantic import BaseModel, Field


class ServiceReviewCreate(BaseModel):
    """
    服务评价创建/更新请求模型
    
    用于提交一次真实服务调用后的结构化评价
    """
    usage_id: int  # 被评价的Usage记录ID

    outcome: str = Field(..., description="评价结果：success / partial / fail")
    task_fit: int = Field(..., ge=1, le=5, description="任务匹配度，1~5")
    output_quality: int = Field(..., ge=1, le=5, description="输出质量，1~5")
    protocol_adherence: int = Field(..., ge=1, le=5, description="协议/格式符合度，1~5")
    would_reuse: bool = Field(..., description="是否愿意再次使用")
    cost_satisfaction: Optional[int] = Field(None, ge=1, le=5, description="性价比满意度，1~5，可空")

    feedback_text: Optional[str] = Field(None, description="可选文本反馈")
    evidence: Optional[Dict[str, Any]] = Field(None, description="可选结构化证据")


class ServiceReviewResponse(BaseModel):
    """
    服务评价响应模型
    
    返回一条评价记录的完整信息
    """
    id: int
    usage_id: int
    service_id: int
    reviewer_user_id: int
    reviewer_agent_id: Optional[int] = None

    outcome: str
    task_fit: int
    output_quality: int
    protocol_adherence: int
    would_reuse: bool
    cost_satisfaction: Optional[int] = None

    feedback_text: Optional[str] = None
    evidence: Optional[Dict[str, Any]] = None

    is_locked: bool
    is_public_aggregate: bool

    created_at: datetime
    updated_at: Optional[datetime] = None

    class Config:
        from_attributes = True


class ServiceTrustSummary(BaseModel):
    """
    服务信任摘要响应模型
    
    返回一个服务当前的聚合信任分和核心统计信息
    """
    service_id: int
    trust_score: float
    objective_score: float
    subjective_score: float

    success_rate: float
    avg_response_time_ms: float

    rating_count: int
    usage_count: int

    last_reviewed_at: Optional[datetime] = None
    last_calculated_at: Optional[datetime] = None

    class Config:
        from_attributes = True
```

------

# 2）新建文件：`app/services/trust_service.py`

这一步先做一个**轻量、可运行**的 trust 计算服务。
不要追求复杂，先保证它能算、能存、能被后面 search 重排调用。

请完整新建下面这份代码：

```python
from datetime import datetime
from typing import Optional

from sqlalchemy.orm import Session

from ..models.usage import Usage
from ..models.review import ServiceReview, ServiceTrustStats


class TrustService:
    """
    服务信任计算服务
    
    负责基于Usage客观数据和ServiceReview主观数据，
    计算并维护服务级别的信任摘要信息
    """

    def __init__(self, db: Session):
        self.db = db

    def _clamp_score(self, value: float) -> float:
        """
        将分值限制在0~100区间
        """
        if value < 0:
            return 0.0
        if value > 100:
            return 100.0
        return round(value, 2)

    def _calc_latency_score(self, avg_response_time_ms: float) -> float:
        """
        根据平均响应时间计算延迟分
        
        第一阶段使用简单分段规则，后续可进一步优化
        """
        if avg_response_time_ms <= 0:
            return 50.0
        if avg_response_time_ms <= 1000:
            return 100.0
        if avg_response_time_ms <= 3000:
            return 80.0
        if avg_response_time_ms <= 5000:
            return 60.0
        if avg_response_time_ms <= 10000:
            return 40.0
        return 20.0

    def _calc_objective_score(self, service_id: int) -> dict:
        """
        计算服务的客观分
        
        基于真实Usage记录，计算成功率、平均响应时间、调用量等指标
        """
        usage_records = self.db.query(Usage).filter(
            Usage.service_id == service_id,
            Usage.is_meaningful == True
        ).all()

        usage_count = len(usage_records)
        if usage_count == 0:
            return {
                "objective_score": 0.0,
                "success_rate": 0.0,
                "avg_response_time_ms": 0.0,
                "usage_count": 0
            }

        success_count = 0.0
        response_times = []

        for usage in usage_records:
            # 成功/部分成功加权
            if usage.final_state == "success":
                success_count += 1.0
            elif usage.final_state == "partial":
                success_count += 0.5

            if usage.execution_time_ms is not None and usage.execution_time_ms >= 0:
                response_times.append(float(usage.execution_time_ms))

        success_rate = (success_count / usage_count) * 100.0
        avg_response_time_ms = sum(response_times) / len(response_times) if response_times else 0.0

        latency_score = self._calc_latency_score(avg_response_time_ms)
        volume_confidence = min((usage_count / 20.0) * 100.0, 100.0)

        objective_score = (
            0.50 * success_rate +
            0.30 * latency_score +
            0.20 * volume_confidence
        )

        return {
            "objective_score": self._clamp_score(objective_score),
            "success_rate": round(success_rate, 2),
            "avg_response_time_ms": round(avg_response_time_ms, 2),
            "usage_count": usage_count
        }

    def _calc_subjective_score(self, service_id: int) -> dict:
        """
        计算服务的主观分
        
        基于结构化评价记录，计算平均主观质量分
        """
        reviews = self.db.query(ServiceReview).filter(
            ServiceReview.service_id == service_id,
            ServiceReview.is_public_aggregate == True
        ).all()

        rating_count = len(reviews)
        if rating_count == 0:
            return {
                "subjective_score": 0.0,
                "rating_count": 0,
                "last_reviewed_at": None
            }

        review_scores = []
        last_reviewed_at = None

        for review in reviews:
            cost_score = review.cost_satisfaction if review.cost_satisfaction is not None else 3

            # 先按1~5评分计算组合分，再映射到0~100
            base_score = (
                0.35 * review.task_fit +
                0.30 * review.output_quality +
                0.20 * review.protocol_adherence +
                0.15 * cost_score
            ) * 20.0

            # 是否愿意再次使用作为轻量修正项
            if review.would_reuse:
                base_score += 5.0
            else:
                base_score -= 5.0

            review_scores.append(self._clamp_score(base_score))

            if review.created_at and (last_reviewed_at is None or review.created_at > last_reviewed_at):
                last_reviewed_at = review.created_at

        subjective_score = sum(review_scores) / len(review_scores)

        return {
            "subjective_score": self._clamp_score(subjective_score),
            "rating_count": rating_count,
            "last_reviewed_at": last_reviewed_at
        }

    def recompute_service_trust(self, service_id: int) -> ServiceTrustStats:
        """
        重算指定服务的信任摘要
        
        这是第一阶段的核心入口：评价提交后或需要展示时调用
        """
        objective_data = self._calc_objective_score(service_id)
        subjective_data = self._calc_subjective_score(service_id)

        # 没有主观评价时，先用客观分作为信任分
        if subjective_data["rating_count"] == 0:
            trust_score = objective_data["objective_score"]
        else:
            trust_score = (
                0.65 * objective_data["objective_score"] +
                0.35 * subjective_data["subjective_score"]
            )

        trust_score = self._clamp_score(trust_score)

        trust_stats = self.db.query(ServiceTrustStats).filter(
            ServiceTrustStats.service_id == service_id
        ).first()

        if not trust_stats:
            trust_stats = ServiceTrustStats(service_id=service_id)
            self.db.add(trust_stats)

        trust_stats.trust_score = trust_score
        trust_stats.objective_score = objective_data["objective_score"]
        trust_stats.subjective_score = subjective_data["subjective_score"]
        trust_stats.success_rate = objective_data["success_rate"]
        trust_stats.avg_response_time_ms = objective_data["avg_response_time_ms"]
        trust_stats.rating_count = subjective_data["rating_count"]
        trust_stats.usage_count = objective_data["usage_count"]
        trust_stats.last_reviewed_at = subjective_data["last_reviewed_at"]
        trust_stats.last_calculated_at = datetime.utcnow()

        self.db.commit()
        self.db.refresh(trust_stats)
        return trust_stats

    def get_service_trust_summary(self, service_id: int) -> Optional[ServiceTrustStats]:
        """
        获取指定服务的信任摘要
        
        如果当前不存在摘要，则现场计算一次
        """
        trust_stats = self.db.query(ServiceTrustStats).filter(
            ServiceTrustStats.service_id == service_id
        ).first()

        if trust_stats:
            return trust_stats

        return self.recompute_service_trust(service_id)
```

------

# 3）修改文件：`app/api/deps.py`

这一步新增一个统一主体依赖：`get_current_principal`。
它和你现在的 `get_current_user()` 不冲突，是并存的。

先把顶部 import 从：

```python
from fastapi import Depends, HTTPException, status
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
from sqlalchemy.orm import Session
```

改成：

```python
from typing import Dict, Any
from fastapi import Depends, HTTPException, status
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
from sqlalchemy.orm import Session
```

然后在文件里新增下面这个函数。
建议放在 `get_current_user()` 后面，`get_current_active_user()` 前面。

```python
def get_current_principal(
    credentials: HTTPAuthorizationCredentials = Depends(security),
    db: Session = Depends(get_db)
) -> Dict[str, Any]:
    """
    获取当前调用主体
    
    与get_current_user不同，该函数会同时返回：
    - user：当前认证用户
    - agent：当前Agent对象（如果是agent_开头的API key），否则为None
    
    Returns:
        Dict[str, Any]: {"user": user, "agent": agent_or_none}
    """
    token = credentials.credentials

    # Agent API key 场景
    if token.startswith("agent_"):
        agent = db.query(Agent).filter(Agent.api_key == token).first()
        if agent is None:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="无效的代理API密钥",
                headers={"WWW-Authenticate": "Bearer"},
            )

        if not agent.is_active:
            raise HTTPException(
                status_code=status.HTTP_400_BAD_REQUEST,
                detail="代理已被禁用"
            )

        if agent.is_suspended:
            raise HTTPException(
                status_code=status.HTTP_400_BAD_REQUEST,
                detail="代理因费用超限被暂停"
            )

        user = db.query(User).filter(User.id == agent.user_id).first()
        if user is None or not user.is_active:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="代理关联的用户不存在或已被禁用",
                headers={"WWW-Authenticate": "Bearer"},
            )

        return {
            "user": user,
            "agent": agent
        }

    # 普通JWT用户场景
    payload = verify_token(token)
    if payload is None:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="无效的访问令牌",
            headers={"WWW-Authenticate": "Bearer"},
        )

    user_identifier = payload.get("sub")
    if user_identifier is None:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="无效的令牌格式",
            headers={"WWW-Authenticate": "Bearer"},
        )

    if isinstance(user_identifier, str) and not user_identifier.isdigit():
        user = db.query(User).filter(User.username == user_identifier).first()
    else:
        user = db.query(User).filter(User.id == int(user_identifier)).first()

    if user is None:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="用户不存在",
            headers={"WWW-Authenticate": "Bearer"},
        )

    if not user.is_active:
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail="用户账户已被禁用"
        )

    return {
        "user": user,
        "agent": None
    }
```

这一步做完后，后面评价接口就能同时支持：

- JWT 用户
- `agent_` API key

------

# 4）新建文件：`app/api/reviews.py`

这一步把评价接口先搭出来。
注意：第一阶段我们先只支持**公开服务**进入公开 trust 体系，所以提交评价时会检查 `service.is_public == True`。

请完整新建下面这份代码：

```python
from datetime import datetime, timedelta

from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.orm import Session

from ..database import get_db
from ..models.usage import Usage
from ..models.service import Service
from ..models.organization import Organization
from ..models.review import ServiceReview
from ..schemas.review import ServiceReviewCreate, ServiceReviewResponse, ServiceTrustSummary
from ..services.trust_service import TrustService
from .deps import get_current_principal

router = APIRouter()

# 第一阶段评价修改窗口：24小时
REVIEW_EDIT_HOURS = 24


@router.post("/", response_model=ServiceReviewResponse)
def create_or_update_review(
    review_data: ServiceReviewCreate,
    principal = Depends(get_current_principal),
    db: Session = Depends(get_db)
):
    """
    提交或更新一次服务评价
    
    评价必须绑定到一条真实的Usage记录，
    同一条Usage只允许保留一条逻辑有效评价
    """
    current_user = principal["user"]
    current_agent = principal["agent"]

    # 1) 查找Usage，确保这条调用属于当前用户
    usage = db.query(Usage).filter(
        Usage.id == review_data.usage_id,
        Usage.user_id == current_user.id
    ).first()

    if not usage:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail="对应的使用记录不存在"
        )

    # 2) 必须是真正进入服务执行阶段的调用
    if not usage.is_meaningful:
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail="该调用未真正进入服务执行阶段，不能评价"
        )

    # 3) 第一阶段只允许对已完成的有效调用评价
    if usage.final_state not in ["success", "partial", "fail"]:
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail="当前调用尚未进入可评价状态"
        )

    # 4) 查找服务
    service = db.query(Service).filter(Service.id == usage.service_id).first()
    if not service:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail="服务不存在"
        )

    # 5) 第一阶段仅将公开服务纳入评价与公开信任体系
    if not service.is_public:
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail="当前阶段仅支持公开服务评价"
        )

    # 6) 禁止服务提供者评价自己组织下的服务
    organization = db.query(Organization).filter(
        Organization.id == service.organization_id
    ).first()

    if organization and organization.owner_id == current_user.id:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail="服务提供者不能评价自己组织下的服务"
        )

    # 7) outcome 合法性校验
    if review_data.outcome not in ["success", "partial", "fail"]:
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail="outcome 仅支持 success / partial / fail"
        )

    # 8) 检查是否已存在评价
    existing_review = db.query(ServiceReview).filter(
        ServiceReview.usage_id == usage.id
    ).first()

    if existing_review:
        # 已锁定则不可修改
        if existing_review.is_locked:
            raise HTTPException(
                status_code=status.HTTP_403_FORBIDDEN,
                detail="该评价已锁定，不能再修改"
            )

        # 超过修改窗口则不可修改
        if existing_review.created_at and existing_review.created_at < datetime.utcnow() - timedelta(hours=REVIEW_EDIT_HOURS):
            raise HTTPException(
                status_code=status.HTTP_403_FORBIDDEN,
                detail="已超过评价修改时间窗口"
            )

        # 更新已有评价
        existing_review.outcome = review_data.outcome
        existing_review.task_fit = review_data.task_fit
        existing_review.output_quality = review_data.output_quality
        existing_review.protocol_adherence = review_data.protocol_adherence
        existing_review.would_reuse = review_data.would_reuse
        existing_review.cost_satisfaction = review_data.cost_satisfaction
        existing_review.feedback_text = review_data.feedback_text
        existing_review.evidence = review_data.evidence
        existing_review.reviewer_agent_id = current_agent.id if current_agent else None

        db.commit()
        db.refresh(existing_review)

        # 重算信任摘要
        trust_service = TrustService(db)
        trust_service.recompute_service_trust(service.id)

        return existing_review

    # 9) 创建新评价
    review = ServiceReview(
        usage_id=usage.id,
        service_id=service.id,
        reviewer_user_id=current_user.id,
        reviewer_agent_id=current_agent.id if current_agent else None,
        outcome=review_data.outcome,
        task_fit=review_data.task_fit,
        output_quality=review_data.output_quality,
        protocol_adherence=review_data.protocol_adherence,
        would_reuse=review_data.would_reuse,
        cost_satisfaction=review_data.cost_satisfaction,
        feedback_text=review_data.feedback_text,
        evidence=review_data.evidence
    )

    db.add(review)
    db.commit()
    db.refresh(review)

    # 10) 重算服务信任摘要
    trust_service = TrustService(db)
    trust_service.recompute_service_trust(service.id)

    return review


@router.get("/services/{service_id}/summary", response_model=ServiceTrustSummary)
def get_service_trust_summary(
    service_id: int,
    principal = Depends(get_current_principal),
    db: Session = Depends(get_db)
):
    """
    获取指定服务的信任摘要
    
    第一阶段主要面向公开服务
    """
    current_user = principal["user"]

    service = db.query(Service).filter(
        Service.id == service_id,
        Service.is_active == True
    ).first()

    if not service:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail="服务不存在"
        )

    if not service.is_public:
        organization = db.query(Organization).filter(
            Organization.id == service.organization_id
        ).first()

        if not organization or organization.owner_id != current_user.id:
            raise HTTPException(
                status_code=status.HTTP_403_FORBIDDEN,
                detail="无权限查看该服务信任摘要"
            )

    trust_service = TrustService(db)
    trust_stats = trust_service.get_service_trust_summary(service_id)

    if not trust_stats:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail="该服务暂无信任摘要"
        )

    return trust_stats


@router.get("/my-usage/{usage_id}", response_model=ServiceReviewResponse)
def get_my_review_by_usage(
    usage_id: int,
    principal = Depends(get_current_principal),
    db: Session = Depends(get_db)
):
    """
    获取当前用户针对某次Usage提交的评价
    
    方便调用方查看或编辑已有评价
    """
    current_user = principal["user"]

    usage = db.query(Usage).filter(
        Usage.id == usage_id,
        Usage.user_id == current_user.id
    ).first()

    if not usage:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail="对应的使用记录不存在"
        )

    review = db.query(ServiceReview).filter(
        ServiceReview.usage_id == usage.id
    ).first()

    if not review:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail="该调用暂无评价记录"
        )

    return review
```

------

# 5）修改文件：`app/main.py`

## 先加 import

找到这一段附近：

```python
from .api import auth, discovery, agents
```

改成：

```python
from .api import auth, discovery, agents, reviews
```

------

## 再注册路由

建议加在 `billing_router` 后面。
加入这段：

```python
# 注册服务评价与信任摘要相关路由
app.include_router(
    reviews.router,
    prefix=f"{settings.API_V1_STR}/reviews",
    tags=["Service Reviews & Trust"]
)
```

------

# 这一步改完后怎么验证

这一步可以做一个**最小闭环验证**。

------

## 验证 1：服务能正常启动

先确认没有 import 或路由注册错误。

------

## 验证 2：OpenAPI 文档里出现 reviews 路由

看 `/docs` 里有没有新分组：

- `Service Reviews & Trust`

里面至少有：

- `POST /api/v1/reviews/`
- `GET /api/v1/reviews/services/{service_id}/summary`
- `GET /api/v1/reviews/my-usage/{usage_id}`

------

## 验证 3：准备一条可评价 usage

你现在现有调用链还没改，但因为 `Usage.final_state` 默认是 `success`，
所以你可以先找一条已有成功调用记录。

先调用：

```bash
GET /api/v1/billing/usage
```

拿一条 `usage_id`。

------

## 验证 4：提交一次评价

示例请求：

```json
POST /api/v1/reviews/
Authorization: Bearer <你的JWT或agent_key>
Content-Type: application/json

{
  "usage_id": 1,
  "outcome": "success",
  "task_fit": 4,
  "output_quality": 4,
  "protocol_adherence": 5,
  "would_reuse": true,
  "cost_satisfaction": 3,
  "feedback_text": "第一阶段测试评价",
  "evidence": {
    "schema_valid": true,
    "downstream_success": true
  }
}
```

成功后应该返回一条 `ServiceReviewResponse`。

------

## 验证 5：查询 trust 摘要

```bash
GET /api/v1/reviews/services/{service_id}/summary
```

应该返回：

- `trust_score`
- `objective_score`
- `subjective_score`
- `success_rate`
- `avg_response_time_ms`
- `rating_count`
- `usage_count`

------

## 这一步先不要处理的问题

先不要急着修：

- 调用响应头里的 `usage_id / request_id`
- sync/stream/async 全部都落 usage
- search 重排
- Tool 返回 trust 字段

这些是第三步和第四步的内容。
现在先把“评价提交 + trust 摘要”这套骨架独立跑起来。

------

你先做完这一步。
做完后把结果告诉我，尤其是这 4 件事：

1. 服务能否正常启动
2. `/docs` 里 reviews 路由是否出现
3. `POST /reviews/` 是否成功提交
4. `GET /reviews/services/{service_id}/summary` 是否成功返回

然后我带你做**第三步：改 proxy 与 billing 链路，让调用后真正返回 `usage_id / request_id`，并让失败/异步调用也能稳定形成可评价锚点**。