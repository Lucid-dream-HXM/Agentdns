我们现在开始为这个项目增加功能的 第一步 。

这一步只做 数据结构准备 ，不改调用逻辑，不碰搜索重排。

第一步目标

先把这三个东西建好：

新增评价模型 ServiceReview

新增信任摘要模型 ServiceTrustStats

给 Usage 和 AsyncTask 补几个后面一定会用到的字段

这样做的原因是：

后面调用完成后评价，必须先有表

搜索重排和 trust 展示，必须先有摘要表

异步任务和 Agent 身份，必须先有字段接住

你现在要改 3 个文件

1）新建文件： app/models/review.py

把下面这份代码完整新建进去。

我已经按你项目原有风格写了中文注释。

from sqlalchemy import Column, Integer, String, DateTime, ForeignKey, Float, Boolean, Text, JSON

from sqlalchemy.sql import func

from sqlalchemy.orm import relationship

from ..database import Base

class ServiceReview(Base):

"""

服务评价模型 - 存储一次真实服务调用后的结构化评价

该模型用于记录用户或Agent在真实调用完成后提交的可选评价，

每条评价必须绑定到一条真实的Usage记录，用于后续服务信任分计算

"""

__tablename__ = "service_reviews"

# 主键字段

id = Column(Integer, primary_key=True, index=True)

# 关联字段

usage_id = Column(Integer, ForeignKey("usage_records.id"), nullable=False, unique=True)  # 一次调用只允许一条有效评价

service_id = Column(Integer, ForeignKey("services.id"), nullable=False)  # 被评价的服务ID

reviewer_user_id = Column(Integer, ForeignKey("users.id"), nullable=False)  # 评价者用户ID

reviewer_agent_id = Column(Integer, ForeignKey("agents.id"), nullable=True)  # 评价者Agent ID，可空

# 评价核心字段

outcome = Column(String(20), nullable=False)  # success / partial / fail

task_fit = Column(Integer, nullable=False)  # 任务匹配度 1~5

output_quality = Column(Integer, nullable=False)  # 输出质量 1~5

protocol_adherence = Column(Integer, nullable=False)  # 协议/格式符合度 1~5

would_reuse = Column(Boolean, nullable=False)  # 是否愿意再次使用

cost_satisfaction = Column(Integer, nullable=True)  # 性价比满意度 1~5，可空

# 辅助信息字段

feedback_text = Column(Text, nullable=True)  # 可选文本反馈

evidence = Column(JSON, nullable=True)  # 可选结构化证据，如schema_valid/downstream_success等

# 状态字段

is_locked = Column(Boolean, default=False)  # 评价是否已锁定，第一阶段先预留

is_public_aggregate = Column(Boolean, default=True)  # 是否可用于公开聚合统计

# 时间戳字段

created_at = Column(DateTime(timezone=True), server_default=func.now())

updated_at = Column(DateTime(timezone=True), onupdate=func.now())

# 模型关联关系

usage = relationship("Usage")

service = relationship("Service")

reviewer_user = relationship("User", foreign_keys=[reviewer_user_id])

reviewer_agent = relationship("Agent", foreign_keys=[reviewer_agent_id])

class ServiceTrustStats(Base):

"""

服务信任摘要模型 - 存储服务级别的聚合信任统计信息

该模型用于存储公开服务的客观分、主观分和综合信任分，

供服务发现重排和服务详情展示直接读取，避免每次临时聚合计算

"""

__tablename__ = "service_trust_stats"

# 服务ID作为主键，一条服务对应一条聚合摘要

service_id = Column(Integer, ForeignKey("services.id"), primary_key=True)

# 信任分字段

trust_score = Column(Float, default=0.0)  # 综合信任分

objective_score = Column(Float, default=0.0)  # 客观分

subjective_score = Column(Float, default=0.0)  # 主观分

# 聚合统计字段

success_rate = Column(Float, default=0.0)  # 成功率

avg_response_time_ms = Column(Float, default=0.0)  # 平均响应时间

rating_count = Column(Integer, default=0)  # 评价数量

usage_count = Column(Integer, default=0)  # 有效调用数量

# 时间字段

last_reviewed_at = Column(DateTime(timezone=True), nullable=True)  # 最近一次评价时间

last_calculated_at = Column(DateTime(timezone=True), nullable=True)  # 最近一次计算时间

# 模型关联关系

service = relationship("Service")

2）修改文件： app/models/usage.py

在 Usage 模型里补 4 个字段。

你找到这段附近：

# 关联字段

user_id = Column(Integer, ForeignKey("users.id"), nullable=False)

service_id = Column(Integer, ForeignKey("services.id"), nullable=False)

在它下面加上：

agent_id = Column(Integer, ForeignKey("agents.id"), nullable=True)  # 发起本次调用的Agent ID，可空

然后在“请求信息字段”或“状态字段”附近，再补这 3 个字段：

http_mode = Column(String(10))  # 调用模式：sync / stream / async

is_meaningful = Column(Boolean, default=True)  # 是否真正进入服务执行阶段，可用于控制是否允许评价

final_state = Column(String(20), default="success")  # 最终状态：success / partial / fail / pending

注意，你当前 usage.py 顶部 import 里没有 Boolean ，所以第一行要从：

from sqlalchemy import Column, Integer, String, DateTime, ForeignKey, Float, JSON, Text

改成：

from sqlalchemy import Column, Integer, String, DateTime, ForeignKey, Float, JSON, Text, Boolean

最后，在模型关联关系里再补一行：

agent = relationship("Agent")

所以 Usage 这一步最少会新增这些内容：

agent_id

http_mode

is_meaningful

final_state

agent relationship

3）修改文件： app/models/async_task.py

给 AsyncTask 补两个字段：

在关联字段区域加：

agent_id = Column(Integer, ForeignKey("agents.id"), nullable=True)  # 创建该异步任务的Agent ID，可空

usage_id = Column(Integer, ForeignKey("usage_records.id"), nullable=True)  # 与该异步任务关联的Usage锚点ID

再在底部关系里补：

agent = relationship("Agent")

usage = relationship("Usage")

这一步改完后怎么处理数据库

因为你已经接受可以重建测试库，所以最简单方式就是：

停掉当前 FastAPI

删除测试数据库里的旧表，或者直接重建数据库

重启服务，让 Base.metadata.create_all() 自动创建新表

如果你现在用的是 PostgreSQL 测试库，最省事的是把数据库重建掉。

如果你暂时不想全删，也可以手动删相关表后重启。

这一步完成后的最低验证

你先不用测接口，只验证两件事：

验证 1：服务能正常启动

确保没有 import 或模型定义报错。

验证 2：数据库里出现了新表和新列

至少确认：

新表：

service_reviews

service_trust_stats

新列：

usage_records.agent_id

usage_records.http_mode

usage_records.is_meaningful

usage_records.final_state

async_tasks.agent_id

async_tasks.usage_id

这一步先不要做的事

先不要同时去改：

proxy.py

billing_service.py

search_engine.py

schemas/

因为现在先把地基搭稳，后面每一步都更清晰。

你先做完这一步，然后把这 3 个文件改完后的状态告诉我。

最好顺手告诉我两件事：

服务能不能正常启动

新表和新列有没有成功创建

然后我带你做 第二步：新增 review schema、trust service 和 reviews API 骨架 。