这个结果是**对的**，而且很有价值。
它说明当前最小实验市场已经足够证明一件事：

> 当候选集小、语义匹配很明确、服务真实差异不大时，trust 机制不会凭空制造优势。

这反而是很好的论文现象：
**trust 的收益依赖于“候选竞争”和“质量分化”这两个前提。**

所以第八步就很明确了：

# 第八步：扩大最小市场差异性，让 V1 / V3 在“竞争候选”里真正拉开差距

这一轮不扩到 192 个服务，也不进大规模主实验。
只做一个**增强版最小市场**，目标是让 V1 和 V3 在一个更真实的“小型竞争市场”里出现稳定差异。

------

# 第八步总目标

做完后，你应该拥有：

1. **10 个最小服务**，而不是 6 个
2. 同类服务之间存在：
   - 高质量高价
   - 中质量中价
   - 低质量低价
   - 漂移型
3. **更模糊的 query**
4. **更有区分度的 warm-up**
5. V1 和 V3 在 evaluation 上开始出现：
   - 不同服务选择
   - 不同 utility
   - 可能不同成本

------

# 为什么现在差异不明显

你当前最小市场的问题，不在闭环，而在**实验张力不够**：

## 1. 同类候选太少

摘要类只有 4 个，抽取类只有 2 个。
而且它们的 query 已经被你修得非常贴合，几乎没有“误选空间”。

## 2. V1 虽然不用 trust，但仍然能靠价格/专精偏好选到好服务

所以即便没有 trust，也没怎么犯错。

## 3. warm-up 还不够“拉开”

虽然有 trust 积累，但没有把**好服务和差服务**明显分出层次。

------

# 第八步怎么做最稳

我建议只做这 4 件事：

## 8A. 把最小市场从 6 个服务扩到 10 个

新增 4 个“更像干扰项”的服务。

## 8B. 修改 query，让 evaluation 更模糊

不要再让 query 一眼就指到唯一最优服务。

## 8C. 修改 V1 的选择策略，使其更接近“无 trust 的现实基线”

也就是少一点“你替它做聪明决策”。

## 8D. 做更有区分度的 warm-up

让便宜但差的服务积累更多负面评价，让高质量服务积累更多正面评价。

------

# 8A：扩充最小市场到 10 个服务

这一轮我建议新增 4 个服务：

## 新增 2 个摘要类

1. `generic-summarizer-cheap-01`
   - 很便宜
   - generalist
   - 质量低
   - 成功率中等
   - 容易 partial
2. `fast-summarizer-drift-01`
   - 速度快
   - 前期看起来不错
   - 后面容易漂移或质量波动

## 新增 2 个抽取类

1. `field-extractor-cheap-01`
   - 便宜
   - schema 遵循一般
   - 容易抽漏字段
2. `robust-json-extractor-01`
   - 比 pro 略便宜
   - 质量较高
   - 形成与 `json-extractor-pro-01` 的真实竞争

这样你最小市场就从：

- 4 个摘要
- 2 个抽取

变成：

- 6 个摘要
- 4 个抽取

这时候 Top-K 才更有竞争性。

------

## 第一步：更新 mock 配置文件

请把 `experiments/configs/services/minimal_variants.json` 末尾再追加下面 4 个对象。

```json
,
  {
    "service_key": "svc_txt_summary_generic_cheap_001",
    "category": "text_generation",
    "subcategory": "summarization",
    "role_type": "generalist",
    "quality_tier": "C",
    "stability_type": "stable",
    "http_mode": "sync",
    "behavior_profile": {
      "base_success_rate": 0.62,
      "base_partial_rate": 0.22,
      "base_fail_rate": 0.16,
      "schema_adherence_rate": 0.72,
      "constraint_follow_rate": 0.58,
      "content_quality_score_mean": 0.48,
      "content_quality_score_std": 0.10
    }
  },
  {
    "service_key": "svc_txt_summary_fast_drift_001",
    "category": "text_generation",
    "subcategory": "summarization",
    "role_type": "generalist",
    "quality_tier": "B",
    "stability_type": "drifting",
    "http_mode": "sync",
    "behavior_profile": {
      "base_success_rate": 0.82,
      "base_partial_rate": 0.10,
      "base_fail_rate": 0.08,
      "schema_adherence_rate": 0.88,
      "constraint_follow_rate": 0.76,
      "content_quality_score_mean": 0.68,
      "content_quality_score_std": 0.08
    }
  },
  {
    "service_key": "svc_ext_json_field_cheap_001",
    "category": "structured_extraction",
    "subcategory": "json_extraction",
    "role_type": "generalist",
    "quality_tier": "C",
    "stability_type": "stable",
    "http_mode": "sync",
    "behavior_profile": {
      "base_success_rate": 0.66,
      "base_partial_rate": 0.18,
      "base_fail_rate": 0.16,
      "schema_adherence_rate": 0.62,
      "constraint_follow_rate": 0.55,
      "content_quality_score_mean": 0.50,
      "content_quality_score_std": 0.09
    }
  },
  {
    "service_key": "svc_ext_json_robust_001",
    "category": "structured_extraction",
    "subcategory": "json_extraction",
    "role_type": "specialist",
    "quality_tier": "A",
    "stability_type": "stable",
    "http_mode": "sync",
    "behavior_profile": {
      "base_success_rate": 0.91,
      "base_partial_rate": 0.05,
      "base_fail_rate": 0.04,
      "schema_adherence_rate": 0.95,
      "constraint_follow_rate": 0.90,
      "content_quality_score_mean": 0.84,
      "content_quality_score_std": 0.05
    }
  }
```

------

## 第二步：新建扩容脚本

新建：

## `experiments/seeds/expand_minimal_market_v2.py`

```python
"""
最小实验市场扩容脚本 v2

作用：
1. 在原有 6 个服务基础上新增 4 个服务
2. 让最小市场扩展为 10 个服务
3. 不改已有主体，只增加竞争候选
"""

from __future__ import annotations

import json
from typing import Any, Dict, List

from sqlalchemy.orm import Session

from app.database import SessionLocal
from app.models.organization import Organization
from app.models.service import Service, ServiceMetadata
from app.models.review import ServiceTrustStats


MOCK_ENDPOINT = "http://127.0.0.1:9001/mock/invoke"


def ensure_service(
    db: Session,
    org: Organization,
    payload: Dict[str, Any]
) -> Service:
    agentdns_uri = f"agentdns://{payload['agentdns_path']}"

    service = db.query(Service).filter(
        Service.agentdns_uri == agentdns_uri
    ).first()
    if service:
        return service

    service = Service(
        name=payload["name"],
        category=payload["category"],
        agentdns_uri=agentdns_uri,
        description=payload["description"],
        version=payload["version"],
        is_active=True,
        is_public=True,
        endpoint_url=MOCK_ENDPOINT,
        protocol="HTTP",
        authentication_required=True,
        pricing_model="per_request",
        price_per_unit=payload["price_per_unit"],
        currency="USD",
        tags=payload["tags"],
        capabilities=payload["capabilities"],
        organization_id=org.id,
        agentdns_path=payload["agentdns_path"],
        http_method="POST",
        http_mode="sync",
        input_description=json.dumps({
            "service_key": payload["capabilities"]["service_key"],
            "task_input": {"text": "string"},
            "stage": "string"
        }, ensure_ascii=False),
        output_description=json.dumps({
            "request_id": "string",
            "service_key": "string",
            "status": "success|partial|fail",
            "result": "object"
        }, ensure_ascii=False)
    )
    db.add(service)
    db.commit()
    db.refresh(service)

    metadata = ServiceMetadata(
        service_id=service.id,
        search_keywords=payload["tags"],
        status="active",
        examples={
            "input": {
                "service_key": payload["capabilities"]["service_key"],
                "task_input": {"text": "这是一个测试输入。"},
                "stage": "cold_start"
            },
            "output": {"status": "success"}
        }
    )
    db.add(metadata)

    trust_stats = ServiceTrustStats(
        service_id=service.id,
        trust_score=0.0,
        objective_score=0.0,
        subjective_score=0.0,
        success_rate=0.0,
        avg_response_time_ms=0.0,
        rating_count=0,
        usage_count=0
    )
    db.add(trust_stats)

    db.commit()
    return service


def main() -> None:
    db: Session = SessionLocal()
    try:
        expnova = db.query(Organization).filter(Organization.name == "expnova").first()
        expatlas = db.query(Organization).filter(Organization.name == "expatlas").first()

        if not expnova or not expatlas:
            raise RuntimeError("未找到 expnova / expatlas，请先运行 seed_minimal_market.py")

        payloads: List[tuple[Organization, Dict[str, Any]]] = [
            (
                expnova,
                {
                    "name": "generic-summarizer-cheap-01",
                    "category": "text_generation",
                    "description": "A generic low-cost summarization service for broad Chinese text compression tasks with moderate quality.",
                    "version": "v1.0",
                    "price_per_unit": 0.02,
                    "tags": [
                        "category:text_generation",
                        "subcategory:summarization",
                        "role:generalist",
                        "mode:sync",
                        "language:zh",
                        "task:document_summary",
                        "budget:very_low_cost"
                    ],
                    "capabilities": {
                        "subcategory": "summarization",
                        "role_type": "generalist",
                        "service_key": "svc_txt_summary_generic_cheap_001",
                        "output_schema_type": "plain_text",
                        "supports_nested_call": False
                    },
                    "agentdns_path": "expnova/text_generation/generic-summarizer-cheap-01"
                }
            ),
            (
                expatlas,
                {
                    "name": "fast-summarizer-drift-01",
                    "category": "text_generation",
                    "description": "A fast general-purpose summarization service with low latency and broad coverage, but less stable output quality over time.",
                    "version": "v1.0",
                    "price_per_unit": 0.04,
                    "tags": [
                        "category:text_generation",
                        "subcategory:summarization",
                        "role:generalist",
                        "mode:sync",
                        "language:zh",
                        "task:document_summary",
                        "latency:fast"
                    ],
                    "capabilities": {
                        "subcategory": "summarization",
                        "role_type": "generalist",
                        "service_key": "svc_txt_summary_fast_drift_001",
                        "output_schema_type": "plain_text",
                        "supports_nested_call": False
                    },
                    "agentdns_path": "expatlas/text_generation/fast-summarizer-drift-01"
                }
            ),
            (
                expatlas,
                {
                    "name": "field-extractor-cheap-01",
                    "category": "structured_extraction",
                    "description": "A low-cost structured field extraction service for simple key-value parsing with moderate reliability.",
                    "version": "v1.0",
                    "price_per_unit": 0.02,
                    "tags": [
                        "category:structured_extraction",
                        "subcategory:json_extraction",
                        "role:generalist",
                        "mode:sync",
                        "language:zh",
                        "task:json_extraction",
                        "budget:very_low_cost"
                    ],
                    "capabilities": {
                        "subcategory": "json_extraction",
                        "role_type": "generalist",
                        "service_key": "svc_ext_json_field_cheap_001",
                        "output_schema_type": "json",
                        "supports_nested_call": False
                    },
                    "agentdns_path": "expatlas/structured_extraction/field-extractor-cheap-01"
                }
            ),
            (
                expnova,
                {
                    "name": "robust-json-extractor-01",
                    "category": "structured_extraction",
                    "description": "A robust Chinese JSON extraction service designed for stable field parsing and reliable structured outputs.",
                    "version": "v1.0",
                    "price_per_unit": 0.05,
                    "tags": [
                        "category:structured_extraction",
                        "subcategory:json_extraction",
                        "role:specialist",
                        "mode:sync",
                        "language:zh",
                        "task:json_extraction"
                    ],
                    "capabilities": {
                        "subcategory": "json_extraction",
                        "role_type": "specialist",
                        "service_key": "svc_ext_json_robust_001",
                        "output_schema_type": "json",
                        "supports_nested_call": False
                    },
                    "agentdns_path": "expnova/structured_extraction/robust-json-extractor-01"
                }
            )
        ]

        created = []
        for org, payload in payloads:
            svc = ensure_service(db, org, payload)
            created.append(svc)

        print("=" * 60)
        print("最小市场 v2 扩容完成")
        print(f"新增服务数: {len(created)}")
        for svc in created:
            print(f"[Service] id={svc.id} name={svc.name} uri={svc.agentdns_uri}")
        print("=" * 60)

    finally:
        db.close()


if __name__ == "__main__":
    main()
```

------

# 8B：把 query 做得更像“竞争查询”

这一步只改 **2 个模板文件**。

## 一）改中等任务模板里的 query

打开：

```
experiments/configs/tasks/medium_task_templates.json
```

把：

### `query_step_1`

从：

```json
"query_step_1": "document summarization chinese"
```

改成：

```json
"query_step_1": "chinese summarization service"
```

把另一个模板里的：

```json
"query_step_2": "study note summarization educational"
```

改成：

```json
"query_step_2": "chinese summary service"
```

把抽取类 query：

```json
"query_step_1": "key value extraction json"
```

和

```json
"query_step_2": "json extraction chinese fields"
```

统一改成更泛一点：

```json
"json extraction service"
```

------

## 二）改 warm-up query

打开：

```
experiments/runners/trust_warmup_runner.py
```

把摘要类 query 统一改成：

```python
"chinese summarization service"
```

把抽取类 query 统一改成：

```python
"json extraction service"
```

这样做的目的只有一个：

> 让 discovery 能召回多个同类服务，但不要提前把唯一最优服务写死在 query 里。

------

# 8C：让 V1 更像真正的“无 trust 基线”

这一点很关键。

你现在 V1 之所以还能选得不错，是因为你在本地选择时仍然给了它不少“聪明规则”。
为了让 V1 和 V3 真正拉开，我建议做一个很小的收缩：

## 修改 `experiments/runners/v1_v3_paired_runner.py`

在 `choose_service()` 里，把 **V1** 的逻辑改成：

- `quality_first`：直接选 `visible[0]`
- `cost_first`：选 Top-K 中最便宜的

这会更贴合“无 trust，仅依赖 discovery 排序和简单成本偏好”的基线。

### 请把函数里 V1 部分改成下面这样：

```python
    if version == "V1":
        if profile == "quality_first":
            # 无 trust 基线：质量型 agent 直接采用 discovery 的 Top-1
            ranked = visible
        elif profile == "cost_first":
            # 成本型 agent：在 Top-K 中选最便宜
            ranked = sorted(visible, key=lambda svc: float(svc.get("price_per_unit") or 0.0))
        else:
            ranked = visible
```

并且把原来 `score_v1_quality` / `score_v1_cost` 这两个函数可以直接删掉，或者留着但不再用。

------

# 8D：让 warm-up 更能“拉开 trust”

当前 warm-up 还不够强。
这一步我建议做一个很小但很有效的改动：

## 修改 `trust_warmup_runner.py`

把 `choose_service_for_warmup()` 的成本型逻辑改成**更偏探索便宜服务**。

把成本型这部分：

```python
    def score_cost(svc: Dict[str, Any]) -> float:
        trust = float(svc.get("trust_score") or 0.0)
        price = float(svc.get("price_per_unit") or 0.0)
        return (10.0 - price * 100.0) + trust * 0.15

    ranked = sorted(visible, key=score_cost, reverse=True)
    return ranked[0]
```

替换成：

```python
    # 成本型 warm-up：更偏向低价探索，让低价服务也积累足够的正负反馈
    ranked = sorted(visible, key=lambda svc: float(svc.get("price_per_unit") or 0.0))
    return ranked[0]
```

这一步会带来一个很有价值的效果：

- 质量型 agent 更常给高质量服务正向评价
- 成本型 agent 更常把低价服务真实“试出来”
- 这样 trust 才会真正分层，而不是大家都只喂给头部服务

------

# 第八步运行顺序

严格按下面顺序来。

## 1. 更新 `minimal_variants.json`

## 2. 运行扩容脚本

```bash
python experiments/seeds/expand_minimal_market_v2.py
```

## 3. 重启 mock service

确保 `/registry` 能看到新增 4 个 `service_key`

```bash
uvicorn experiments.mock_services.app:app --host 0.0.0.0 --port 9001 --reload
```

## 4. 修改中等任务 query

## 5. 修改 warm-up runner

## 6. 修改 V1 选择逻辑

------

## 然后正式重跑“预热后对比”

### 7. reset

```bash
python experiments/seeds/reset_minimal_experiment_state.py
```

### 8. 删除旧对比结果

删除：

- `experiments/outputs/runs/v1_vs_v3_runs.jsonl`
- `experiments/outputs/runs/v1_vs_v3_summary.csv`

### 9. warm-up

```bash
python experiments/runners/trust_warmup_runner.py
```

### 10. 跑 V1

```bash
python experiments/runners/v1_v3_paired_runner.py V1
```

### 11. reset

```bash
python experiments/seeds/reset_minimal_experiment_state.py
```

### 12. 再 warm-up

```bash
python experiments/runners/trust_warmup_runner.py
```

### 13. 跑 V3

```bash
python experiments/runners/v1_v3_paired_runner.py V3
```

### 14. 汇总

```bash
python experiments/analyzers/v1_v3_summary.py
```

### 15. 出图

```bash
python experiments/analyzers/plot_v1_v3_comparison.py
```

------

# 第八步你要重点看什么

请重点看这 6 项：

1. 最小市场是否已从 6 个服务扩到 10 个
2. `/registry` 是否能看到新增 `service_key`
3. warm-up 后 trust 是否出现更明显分层
4. V1 和 V3 是否开始选出不同服务组合
5. `v1_vs_v3_summary.csv` 里：
   - 平均 oracle utility
   - 平均总成本
   - 任务成功率
     是否开始出现差异
6. 质量优先型和成本优先型 Agent 是否比之前更分化

------

# 这一步完成的意义

如果第八步跑出来差异了，那你就会第一次拿到一个非常像论文结果的现象：

> 在候选竞争更强、服务质量分层更明显的小型市场中，静态 trust 机制开始影响服务选择，并在 utility 或成本—质量权衡上优于无 trust 基线。

这就是非常好的主实验雏形。

------

你做完第八步后，回我这 6 项结果，我再带你进入：

**第九步：把这套最小对比实验收成“可重复一键运行 + 可直接放论文的表述模板”**。